import { TabToNotesPipe } from './tab-to-notes.pipe';

describe('TabToNotesPipe', () => {
  it('create an instance', () => {
    const pipe = new TabToNotesPipe();
    expect(pipe).toBeTruthy();
  });
});
